// some semantic test cases 
int main()
{
    int min = 0, max = -1, mid = -1;
    if(min == 0) {
        if (pos) pos = 0;  // undeclared variable
        return 0;
    }
    else {
        if(max > 5)
        {
            return 0;
        }
    }
    
    while(max >= min) {
        mid = (max + min) / 2;
        max = max - 1;
    }
    return  50;

}